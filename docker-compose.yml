services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - app-data:/data
    environment:
      - DB_PATH=/data/recruiter.db
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
    restart: unless-stopped

volumes:
  app-data:
